from django.test import TestCase

# TODO: write test for following functionalities
# Registration: successful registration (201), invalid email (400), short password (400),
# Current User: authorized (200), unauthorized (401)
# Logout: valid refresh token (205), invalid refresh token (400)
# Delete User: deleting account (204), attempt without authorization (401)
